spring.datasource.url=jdbc:h2:mem:db;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create
fixtures.users.test1.email=${TEST1_EMAIL}
fixtures.users.test1.password=${TEST1_PASSWORD}
fixtures.users.test2.email=${TEST2_EMAIL}
fixtures.users.test2.password=${TEST2_PASSWORD}
